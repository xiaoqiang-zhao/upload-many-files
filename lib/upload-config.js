/**
 * 上传配置
 */

const dataFolderPath = __dirname + '/../data/';

module.exports = {
    dataFolderPath,
    dataConfigPath: dataFolderPath + 'config.json',
    jobsMainPath: dataFolderPath + 'jobs-main.json',
};
